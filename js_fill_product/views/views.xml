<odoo>
    <data>
        <!-- Botón para descargar las imágenes -->
        <record id="view_product_form_inherit" model="ir.ui.view">
            <field name="name">product.template.common.form.inherit</field>
            <field name="model">product.template</field>
            <field name="inherit_id" ref="product.product_template_form_view"/>
            <field name="arch" type="xml">
                <button name="toggle_active" position="before">
                    <button class="oe_stat_button" name="js_download_images" string="Descargar imágenes"
                        type="object" icon="fa-cloud-download" attrs="{'invisible': [('sale_ok','=',False)]}">
                        <!-- AQUÍ INFORMARÁ DEL ESTADO DE LA DESCARGA -->
                    </button>
                </button>
            </field>
        </record>
    </data>

    <!-- Página de resultados (batch_update)  -->
    <template id="js_fill_product_batch" name="JS Fill Product Batch">
        <h3>Procesados <t t-esc="total"/> productos</h3>

        <t t-foreach="products" t-as="p">
            <p>
                <t t-esc="p['reference']"/> <t t-esc="p['name']"/> 
                <t t-if="p['images'][0] > 0" t-esc="p['images']"/>
                <t t-else=""><strong>FAIL!</strong></t>
            </p>
        </t>
    </template>
</odoo>
