<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="js_res_partner_form" model="ir.ui.view">
        <field name="name">js.res.partner.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="type">form</field>
        <field name="priority" eval="25" />
        <field name="arch" type="xml">
            <!-- El SIRET es obligatorio cuando el contacto es una empresa -->
            <xpath expr="//field[@name='siret']" position="replace">
                <field name="siret" attrs="{'invisible': [('is_company', '=', False)],'required': [('is_company', '=', True)],}"/>
            </xpath>
        </field>
    </record>
</odoo>